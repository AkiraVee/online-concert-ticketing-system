Create Database ticketdb;
show databases;
use ticketDb;

Create TABLE usertb
(
UserID int(8) PRIMARY KEY AUTO_INCREMENT,
FullName varchar(30),
Email varchar(30),
Birthdate date,
Age int(2),
ContactNumber varchar (11),
password varchar(50)
);


Create TABLE ordertb
(
TicketID int(8) PRIMARY KEY AUTO_INCREMENT,
UserID int(8),
EventTitle varchar(50),
SeatLocation varchar(50),
EventDate date,
TicketQuantity int(5),
TotalPrice int(50),
TransactionPin varchar(8),
PurchaseDate date,
EventLocation varchar(100),
PaymentMethod varchar(20)
);

$insertQuery = "insert into ordertb (UserID, EventTitle, SeatLocation, EventDate,  TicketQuantity, TotalPrice, TransactionPin, PurchaseDate, EventLocation, PaymentMethod)
            values ($userID, '$eventTitle', '$tier', '$parsedDate', '$quantity', '$grandTotal', '$transactionPin', NOW(), '$eventLocation', '$paymentMethod')";


